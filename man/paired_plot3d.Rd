% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired_plot3d.R
\name{paired_plot3d}
\alias{paired_plot3d}
\title{Draw a paired (pre/post) comparison on two variables as a 3D "spaghetti" plot}
\usage{
paired_plot3d(
  data,
  x_pre,
  x_post,
  z_pre,
  z_post,
  x_name = NULL,
  z_name = NULL,
  conf.level = 0.95,
  opacity = 0.75,
  col_pre = "#2a78d6",
  col_post = "#eb6834",
  col_dir = c("#1baf7a", "#d03b3b", "#9a9890")
)
}
\arguments{
\item{data}{A data frame in wide format: one row per subject, with
separate columns for the Pre and Post measurement of each variable.}

\item{x_pre, x_post}{Character. Column names of the first variable's Pre
and Post measurements.}

\item{z_pre, z_post}{Character. Column names of the second variable's Pre
and Post measurements.}

\item{x_name, z_name}{Character. Axis titles for the two variables
(default: derived from \code{x_pre}/\code{z_pre} by stripping a trailing
\verb{_pre}/\verb{_Pre} suffix if present).}

\item{conf.level}{Confidence level for the paired t-tests (default 0.95).}

\item{opacity}{Initial opacity (0-1) of the subject lines (default 0.75);
adjustable afterwards with the transparency slider.}

\item{col_pre, col_post}{Colors for the Pre and Post markers.}

\item{col_dir}{Length-3 character vector of colors for the "Direccion de
cambio" coloring scheme: both axes increase, both decrease, mixed
change (in that order).}
}
\value{
A \code{plotly} htmlwidget object (auto-displayed if the result isn't
assigned). The full statistics -- \code{n}, per-variable Pre/Post means and
SDs, \code{t_x} and \code{t_z} (paired \code{\link[stats:t.test]{stats::t.test()}} results), and
\code{hotelling_paired} (a list with \code{T2}, \code{F}, \code{df1}, \code{df2}, \code{p.value}) --
are attached as the \code{"stats"} attribute.
}
\description{
A repeated-measures counterpart to \code{\link[=ttest_plot3d]{ttest_plot3d()}}: instead of two
independent groups, the same subjects are measured twice (e.g. before and
after an intervention) on two continuous variables. Each subject is drawn
as a point at their "Pre" measurement and a point at their "Post"
measurement, joined by a line, in a 3D scene where \code{x} and \code{z} are the two
variables and the third axis ("Momento") separates Pre from Post. Both
paired Student's t-tests (one per variable) and a joint one-sample
(paired) Hotelling's T-squared test on the vector of differences are
reported.
}
\examples{
set.seed(1)
n <- 20
df <- data.frame(
  x_pre = rnorm(n, 60, 9), z_pre = rnorm(n, 18, 4)
)
df$x_post <- df$x_pre - rnorm(n, 9, 6)
df$z_post <- df$z_pre - rnorm(n, 6, 4)
paired_plot3d(df, x_pre = "x_pre", x_post = "x_post",
              z_pre = "z_pre", z_post = "z_post")

}
