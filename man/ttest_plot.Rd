% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ttest_plot.R
\name{ttest_plot}
\alias{ttest_plot}
\title{Draw a t-distribution curve with the observed statistic and critical region}
\usage{
ttest_plot(
  x = NULL,
  y = NULL,
  mu = 0,
  paired = FALSE,
  alternative = c("two.sided", "less", "greater"),
  var.equal = FALSE,
  conf.level = 0.95,
  t = NULL,
  df = NULL,
  col_curve = "#2a78d6",
  col_critical = "#d03b3b",
  col_observed = "#0b0b0b",
  main = NULL,
  lang = c("es", "en")
)
}
\arguments{
\item{x}{Numeric vector (first sample), or \code{NULL} if using the manual
\code{t}/\code{df} mode.}

\item{y}{Optional numeric vector (second sample), for a two-sample or
paired test.}

\item{mu}{Number. Null-hypothesis mean for a one-sample test (default 0).}

\item{paired}{Logical. Paired t-test (default \code{FALSE}).}

\item{alternative}{One of \code{"two.sided"} (default), \code{"less"}, \code{"greater"}.}

\item{var.equal}{Logical. Assume equal variances in a two-sample test
(default \code{FALSE}, i.e. Welch).}

\item{conf.level}{Confidence level defining the critical region
(default 0.95, i.e. alpha = 0.05).}

\item{t, df}{Numeric. Manual mode: the observed t-statistic and degrees
of freedom, used instead of computing a test from \code{x}/\code{y}. Both must
be supplied together.}

\item{col_curve, col_critical, col_observed}{Colors for the density curve,
the shaded critical region, and the observed-statistic marker.}

\item{main}{Optional plot title.}

\item{lang}{One of \code{"es"} (default) or \code{"en"}, translating the axis
labels, legend text, and error message.}
}
\value{
Invisibly, a list with \code{t}, \code{df}, \code{p.value}, \code{alternative},
\code{alpha}, \code{critical} (the critical value(s)), and \code{stars}.
}
\description{
A didactic plot: the t-distribution density curve for the relevant
degrees of freedom, the critical (rejection) region shaded according to
\code{alternative} and \code{conf.level}, and the observed t-statistic marked with
a vertical line -- so it's visually clear whether the observed value
falls inside the rejection region.
}
\details{
Two ways to use it:
\itemize{
\item \strong{From data}: pass \code{x} (and optionally \code{y}) as you would to
\code{\link[stats:t.test]{stats::t.test()}} -- one-sample, two-sample, or paired -- and the
test is run internally.
\item \strong{Manual}: pass \code{t} and \code{df} directly (skipping \code{x}/\code{y}
entirely), useful for teaching a specific scenario without needing
raw data.
}
}
\examples{
set.seed(1)
a <- rnorm(20, 5, 1)
b <- rnorm(20, 6, 1)
ttest_plot(a, b)

# manual mode: no data needed
ttest_plot(t = 2.4, df = 28, alternative = "greater")

}
