% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplot3d_significance.R
\name{boxplot3d_significance}
\alias{boxplot3d_significance}
\title{Significance testing for a 3-axis (triaxial) box plot}
\usage{
boxplot3d_significance(
  data,
  x = "x",
  y = "y",
  z = "z",
  group = "group",
  parametric = 1,
  nonparametric = 1,
  scale = 0,
  nperm = 999,
  seed = 1
)
}
\arguments{
\item{data}{A data frame containing the three axes and the grouping
variable.}

\item{x, y, z}{Character. Column names of the three continuous axes.}

\item{group}{Character. Column name of the grouping variable.}

\item{parametric, nonparametric}{0/1. Which family (or both) of tests to
run. At least one must be 1.}

\item{scale}{0/1. If 1, standardize (z-score) \code{x}, \code{y}, \code{z} before
testing, so axes on different units/scales contribute comparably to
the joint (3D) test. Per-axis and MANOVA results are invariant to
this; PERMANOVA is not, since it is based on Euclidean distance.}

\item{nperm}{Integer. Number of permutations for the PERMANOVA test.}

\item{seed}{Integer. Random seed for the permutations (reproducibility).}
}
\value{
A data frame with one row per axis plus one row for the joint
3D comparison, with the test names, p-values, and significance stars.
}
\description{
Compares groups on three continuous axes at once, both per-axis and
jointly (3D), using a parametric test and a non-parametric test in
parallel (not mutually exclusive -- set both \code{parametric} and
\code{nonparametric} to 1 to get both, as is the default).
}
\details{
Parametric: Welch t-test (2 groups) / Welch ANOVA (>2 groups) per axis;
MANOVA (Pillai's trace) for the joint 3D comparison.

Non-parametric: Wilcoxon rank-sum test (2 groups) / Kruskal-Wallis
(>2 groups) per axis; a permutation-based PERMANOVA (one-way, Euclidean
sum-of-squares decomposition, computed without depending on 'vegan')
for the joint 3D comparison.
}
\examples{
set.seed(1)
df <- rbind(
  data.frame(x = rnorm(30, 0), y = rnorm(30, 0), z = rnorm(30, 0), group = "A"),
  data.frame(x = rnorm(30, 1), y = rnorm(30, 1), z = rnorm(30, 1), group = "B")
)
boxplot3d_significance(df, nperm = 199)

}
