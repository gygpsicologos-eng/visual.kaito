% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplot3d.R
\name{boxplot3d}
\alias{boxplot3d}
\title{Draw a 3-axis (triaxial) box plot}
\usage{
boxplot3d(
  data,
  x = "x",
  y = "y",
  z = "z",
  group = "group",
  tukey = 1,
  percentile = 0,
  sd = 0,
  letter_value = 0,
  k = 1.5,
  probs = c(0.05, 0.95),
  sd_mult = c(1, 2),
  col = NULL
)
}
\arguments{
\item{data}{A data frame with the three axes and the grouping variable.}

\item{x, y, z}{Character. Column names of the three continuous axes.}

\item{group}{Character. Column name of the grouping variable.}

\item{tukey, percentile, sd, letter_value}{0/1 flags selecting the
box/whisker convention. Exactly one must be 1.}

\item{k}{Numeric. Tukey fence multiplier (default 1.5).}

\item{probs}{Length-2 numeric. Lower/upper whisker percentiles for the
\code{percentile} convention (default \code{c(0.05, 0.95)}).}

\item{sd_mult}{Length-2 numeric. Box and whisker multipliers (in SDs)
for the \code{sd} convention (default \code{c(1, 2)}).}

\item{col}{Optional character vector of colors, one per group.}
}
\value{
Invisibly, a list with one element per group, each a list of
per-axis statistics (\code{q1}, \code{q3}, \code{center}, \code{whisker_lo}, \code{whisker_hi},
plus \code{outliers}, a logical vector flagging points outside the
whiskers on that axis).
}
\description{
Computes box-and-whisker statistics on three continuous axes at once,
for each group in \code{group}, and draws them as three linked pairwise
panels (axis 1 vs 2, 2 vs 3, 1 vs 3) using base graphics.
}
\details{
Exactly one of \code{tukey}, \code{percentile}, \code{sd}, \code{letter_value} must be 1
(the rest 0): this selects how the box and whiskers are defined.
\itemize{
\item \strong{tukey} (default): box = P25-P75; whiskers reach the most
extreme data point within \code{k} * IQR of the box; points beyond
that fence are drawn individually as outliers.
\item \strong{percentile}: box = P25-P75; whiskers reach fixed
percentiles (\code{probs}), regardless of outliers.
\item \strong{sd}: centered on the mean; box = mean +/- \code{sd_mult[1]}
SD; whiskers = mean +/- \code{sd_mult[2]} SD.
\item \strong{letter_value}: nested boxes at successively finer
percentile depths (Tukey's letter values), for large samples.
}

This base-graphics version draws three 2D projections rather than a
single rotatable 3D scene; an interactive rotatable version (with the
same four conventions, a notch option, a same-scale/z-score option,
and axis-pair view switching) exists as a prototype and is planned for
a future release of this package.
}
\examples{
set.seed(1)
df <- rbind(
  data.frame(x = rnorm(30, 0), y = rnorm(30, 0), z = rnorm(30, 0), group = "A"),
  data.frame(x = rnorm(30, 1), y = rnorm(30, 1), z = rnorm(30, 1), group = "B")
)
boxplot3d(df)

}
