% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manova_plot3d.R
\name{manova_plot3d}
\alias{manova_plot3d}
\title{Draw more-than-2 groups as bivariate 3D "mountains" with pairwise post-hoc tests}
\usage{
manova_plot3d(
  data,
  x = "x",
  z = "z",
  group = "group",
  control = NULL,
  conf.level = 0.95,
  n_grid = 50,
  opacity = 0.5,
  col = NULL,
  col_overlap = "#4a3aa7"
)
}
\arguments{
\item{data}{A data frame with the two continuous axes and the grouping
variable.}

\item{x, z}{Character. Column names of the two continuous axes.}

\item{group}{Character. Column name of the grouping variable. Must have
more than 2 levels (with exactly 2, use \code{\link[=ttest_plot3d]{ttest_plot3d()}} instead).}

\item{control}{Character. The reference group for Dunnett's test. If
\code{NULL} (default), the first level of \code{group} is used and a message
reports which one that is.}

\item{conf.level}{Confidence level (default 0.95).}

\item{n_grid}{Integer. Grid resolution per axis for the density surfaces
(default 50).}

\item{opacity}{Initial opacity (0-1) of the group surfaces (default 0.5);
adjustable afterwards with the transparency slider.}

\item{col}{Character vector of colors, one per group. Defaults to the
Okabe-Ito palette.}

\item{col_overlap}{Color used to mark the overlap of the currently
compared pair.}
}
\value{
A \code{plotly} htmlwidget object (auto-displayed if the result isn't
assigned). Full statistics -- \code{manova} (Wilks' lambda test), \code{means},
\code{covariances}, and \code{posthoc} (a data frame with one row per pair per
method: per-axis p-values, the pair's Hotelling T2 p-value, and whether
that method applies to that pair) -- are attached as the \code{"stats"}
attribute.
}
\description{
The many-groups counterpart to \code{\link[=ttest_plot3d]{ttest_plot3d()}}. Each group (there must be
more than 2) is drawn as its own bivariate density surface ("mountain")
over the (\code{x}, \code{z}) plane, all in the same 3D scene. An overall one-way
MANOVA (Wilks' lambda) tests whether the groups differ jointly on both
variables; a "Comparar" menu picks which pair of groups to highlight (their
overlap is shaded, same convention as \code{\link[=ttest_plot3d]{ttest_plot3d()}}), and a "Metodo"
menu reports that pair's post-hoc result under Bonferroni, Tukey (HSD),
DMS (Fisher's LSD) or Dunnett.
}
\details{
Tukey and Dunnett do not have a standard closed-form multivariate version,
so the 4 post-hoc methods are computed axis-by-axis with the real
underlying R routines (\code{\link[stats:TukeyHSD]{stats::TukeyHSD()}}, \code{\link[stats:pairwise.t.test]{stats::pairwise.t.test()}}
with Bonferroni or no correction, and Dunnett via the \code{multcomp} package)
-- the same per-axis convention \code{\link[=ttest_plot3d]{ttest_plot3d()}} already uses for its two
t-tests. Each pair's unadjusted, 2-variable Hotelling's T-squared (as in
\code{\link[=ttest_plot3d]{ttest_plot3d()}}) is also reported for every method, as a joint summary
of that one pair.

Dunnett compares every group only against a fixed reference/\code{control}
group, not all pairs against each other -- selecting a pair that does not
include \code{control} while "Metodo" is set to Dunnett shows a note instead of
a p-value for that pair.
}
\examples{
set.seed(1)
df <- rbind(
  data.frame(x = rnorm(25, 0, 1.2), z = rnorm(25, 0, 1.1), group = "Control"),
  data.frame(x = rnorm(25, 2, 1.3), z = rnorm(25, 1.4, 1.2), group = "A"),
  data.frame(x = rnorm(25, 2.5, 1.1), z = rnorm(25, 3.2, 1.3), group = "B")
)
manova_plot3d(df, control = "Control")

}
